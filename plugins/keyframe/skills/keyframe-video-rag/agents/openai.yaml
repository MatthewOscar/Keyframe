interface:
  display_name: "Keyframe Video RAG"
  short_description: "Find and analyze timestamped video evidence"
  default_prompt: "Use $keyframe-video-rag to preserve my topic, find a strongly relevant public video when I provide no source, and analyze it with timestamp citations."
policy:
  allow_implicit_invocation: false
