interface:
  display_name: "Keyframe Video RAG"
  short_description: "Fast, timestamped video and GIF evidence"
  default_prompt: "Use $keyframe-video-rag to preserve my topic and requested evidence, analyze short videos in one pass when possible, and cite timestamps."
policy:
  allow_implicit_invocation: false
